[project]
name = "gencv003"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "abdulrahman1238", email = "bodyabokamar100@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
    "pillow>=12.3.0",
    "pytorch-fid>=0.3.0",
    "pyyaml>=6.0.3",
    "torch>=2.5.1",
    "torchmetrics>=1.9.0",
    "torchvision>=0.20.1",
    "tqdm>=4.70.0",
]
[tool.uv]
package = false

[project.scripts]
gencv003 = "gencv003:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
